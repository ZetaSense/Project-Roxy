[gd_scene load_steps=4 format=3 uid="uid://j8b4gcuf5t1j"]

[ext_resource type="Texture2D" uid="uid://c8dpnebkdxffs" path="res://Enemies assets/Anxiety_2.png" id="1_sgx5y"]

[sub_resource type="GDScript" id="GDScript_wihx7"]
resource_name = "Anxitey_Enemy"
script/source = "extends Node2D

const WALK_SPEED = 100
const GRAVITY = 600
const JUMP_VELOCITY = -400

@export var health = 1
onready var Students = get.node(\"/root/world/student\")

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _physics_process(delta):
	if Students:
		#Get direction (Vector2) from current enemy's position to player's postition
		var direction = (Students.position - postion).normalized()
		if not is_on_floor():
			direction.y += GRAVITY
		
		move_and_slide(direction - WALK_SPEED)
		
		for i in get_slide_count():
			var collection = get_slide_collision(i)
			#if collsion.collider.name == \"Studnets\":
				#print(\"dead\")
			var object =collison. collider
			if object.is_in_group(\"Students\"):
				object.damage(1)
			elif object.is_in_group(\"Students\"):
				object.die()
				#Print gave over screen on menu
"

[sub_resource type="RectangleShape2D" id="RectangleShape2D_m2lea"]
size = Vector2(1488, 2057.42)

[node name="Node2D" type="Node2D"]
script = SubResource("GDScript_wihx7")

[node name="Anxiety" type="Sprite2D" parent="."]
scale = Vector2(0.240592, 0.240592)
texture = ExtResource("1_sgx5y")

[node name="Area2D" type="Area2D" parent="Anxiety"]

[node name="CollisionShape2D" type="CollisionShape2D" parent="Anxiety/Area2D"]
position = Vector2(12.4692, -14.5477)
scale = Vector2(0.999999, 1)
shape = SubResource("RectangleShape2D_m2lea")
