[gd_scene load_steps=4 format=3 uid="uid://dfeoc6ec1toqu"]

[ext_resource type="Texture2D" uid="uid://ci1xs45ctmbia" path="res://Enemies assets/Frustration.png" id="1_xkwnc"]

[sub_resource type="GDScript" id="GDScript_4itm0"]
resource_name = "Frustartion"
script/source = "extends Node2D

const WALK_SPEED = 150
const GRAVITY = 600
const JUMP_VELOCITY = -400

@export var health = 1
onready var Students = get.node(\"/root/World/Students\")

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _physics_process(delta):
	if Students:
		#Get direction (Vector2) from current enemy's position to player's postition
		var direction = (Students.position - postion).normalized()
		if not is_on_floor():
			direction.y += GRAVITY
		
		move_and_slide(direction - WALK_SPEED)
		
		for i in get_slide_count():
			var collection = get_slide_collision(i)
			#if collsion.collider.name == \"Studnets\":
				#print(\"dead\")
			var object = collison. collider
			if object.is_in_group(\"Students\"):
				object.damage(1)
			elif object.is_in_group(\"Students\"):
				object.die()
				#Print gave over screen on menu
			
"

[sub_resource type="RectangleShape2D" id="RectangleShape2D_8fqmm"]
size = Vector2(1169.46, 1610.3)

[node name="Node2D" type="Node2D"]
script = SubResource("GDScript_4itm0")

[node name="Frustration" type="Sprite2D" parent="."]
scale = Vector2(0.326646, 0.326646)
texture = ExtResource("1_xkwnc")

[node name="Area2D" type="Area2D" parent="Frustration"]

[node name="CollisionShape2D" type="CollisionShape2D" parent="Frustration/Area2D"]
position = Vector2(-128.579, -168.378)
shape = SubResource("RectangleShape2D_8fqmm")
